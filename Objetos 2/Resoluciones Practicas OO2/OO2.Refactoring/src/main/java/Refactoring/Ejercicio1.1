Ejercicio1.1
Indicar malos olores

/** 
* Retorna el límite de crédito del cliente
*/
public double lmtCrdt() {...

/** 
* Retorna el monto facturado al cliente desde la fecha f1 a la fecha f2
*/
protected double mtFcE(LocalDate f1, LocalDate f2) {...

/** 
* Retorna el monto cobrado al cliente desde la fecha f1 a la fecha f2
*/
private double mtCbE(LocalDate f1, LocalDate f2) {...
					

Bad Smells: 
El uso de comentarios obviamente es un mal olor, habria que hacer que el codigo sea claro por si solo para de esta forma no tener que utilizar comentarios que indican lo que el codigo hace/debe hacer.
Para solucionar esto entiendo habria que empezar por modificar los nombres de los metodos y usar nombres que tengan que ver con la funcion que realiza el metodo. 
Nombres posibles:
	-getLimiteCredito o retornarLimiteCredito
	-retornarMontoFacturadoEnPeriodo (El uso de nombres largos para identificar un metodo puede que sea un mal olor?)
	-retornarMontoCobradoEnPeriodo 
Tambien veo como una buena herramienta el crear una clase periodo que utilice dos fechas para de esta forma achicar la lista de parametros que usan los metodos, esta clase podria tener un metodo booleano que verifique cuando una fecha esta dentro del periodo. 
 
bad smells:
Long Parameter List 
Data clumps
